[build]
  command = "npm install && cd client && npm install && npm run build"
  publish = "client/build"          # React build output
  functions = "netlify/functions"   # Functions are in netlify/functions at the root

[[redirects]]
  from = "/api/cart"
  to = "/.netlify/functions/shopifyCart"
  status = 200
  force = true