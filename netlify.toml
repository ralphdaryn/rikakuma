[build]
  base = "client"
  command = "npm install && npm run build"  # Fix: No "cd client"
  publish = "build"  # Fix: No "client/build", since we're already in "client"
  functions = "../netlify/functions"  # Fix: Reference functions from the root