[build]
  publish = "client/build"          # The output folder for your React app
  functions = "netlify/functions"   # Your functions folder at the repo root
  command = "npm install && cd client && npm install && npm run build"

[[redirects]]
  from = "/api/cart"
  to = "/.netlify/functions/shopifyCart"
  status = 200
  force = true